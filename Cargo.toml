# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
# !!! CRITICAL WARNING - DO NOT MODIFY BINARY TARGETS              !!!
# !!! This project must ONLY build the following executable:       !!!
# !!! 1. http_tunnel_server - The HTTP tunnel server                !!!
# !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

[workspace]
members = ["standalone_server"]
default-members = ["standalone_server"]

[package]
name = "tagio_workspace"
version = "0.2.1"
edition = "2021"
authors = ["TagIO Team"]
description = "TagIO HTTP Tunnel Server - Standalone HTTP tunneling server for TagIO"

# This is just a placeholder to satisfy Cargo requirements
[lib]
name = "tagio_workspace_lib"
path = "src/lib.rs"

[profile.release]
opt-level = "z"     # Optimize for size, not speed
lto = true          # Enable Link Time Optimization
codegen-units = 1   # Use a single codegen unit for better optimization
strip = true        # Strip symbols from binary
panic = "abort"     # Remove panic unwinding code
overflow-checks = false  # Disable integer overflow checks
debug = false       # No debug info at all

# Optimize dependencies as well
[profile.release.package."*"]
opt-level = "z"     # Optimize all dependencies for size too
debug = false

[profile.dev]
opt-level = 0
debug = 1  # Less debug info for faster builds
incremental = true

# Optimize dependencies even in debug builds
[profile.dev.package."*"]
opt-level = 1
debug = false 
